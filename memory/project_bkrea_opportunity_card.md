---
name: project_bkrea_opportunity_card
description: "BKREA Opportunity card redesign — buildout bar per use, green theme, movement dropdown; air-rights is a parked future item"
metadata: 
  node_type: memory
  type: project
  originSessionId: c3e8ae92-3911-481d-b7fa-c3a885743b7a
  modified: 2026-07-26T16:15:06.714Z
---

**Opportunity card (buildable analysis) redesign — Login-approved 2026-07-24.** In `components/map/PropertyCard.tsx`, the `<Section title="Opportunity">` "Scenarios" block. GREEN/lime theme (kept distinct from blue Development, purple Comparables).

**Design:** replace the old table (Use · Remaining SF · Net FAR · % Built — where Net FAR was REDUNDANT with Remaining SF, = remaining ÷ lot) with a **bar-per-use** layout. Each use (Residential, Office, …) is a row:
- Collapsed: use label · remaining-SF headline (lime if the leading use) · a **buildout bar** (dark segment = existing/built, lime segment = remaining upside; full width = MAX envelope, so the fill IS % built) · "X built · Z% · max Y SF".
- Tap → **dropdown = Option A "movement →"**: a small table, one row per metric — `SF: existing → max (+remaining)`, `FAR: existingFAR → maxFAR (+remaining)`, `Built: %built → 100% (+movement%)`. This shows existing/maximum/remaining across the 3 metrics (SF/FAR/%) AND the percent movement Login wanted. THEN below it, the calcs explanation (the derivation: envelope = FAR × lot − existing = remaining; QRS/City-of-Yes notes for rezoning rows).
- Chose Option A (movement arrows) over Option B (3×3 grid) — Login: likes the percent movement / growth-story framing.
- Group rows (Portfolio / Assemblage / QRS rezoning) use the SAME bar renderer. Data: existing = `totals.existingSf` (portfolio & assemblage both have it, lib/assemblages.ts); remaining/lot from their totals; QRS rows reuse the same existingSf (only the envelope/FAR changes).

**SCENARIO WIDGET — final form (shipped 2026-07-24, commits through ff056b7):** each scenario is a bordered widget: TITLE only (no color swatch — grouping carries site/multi/rezone distinction), then a 3-row movement grid `SF · FAR · Built` showing `existing → max`, with each **delta widgeted as its own lime pill** in the last column (+SF, +FAR, +% built). No progress bars (removed — read as confusing). Plain use rows have NO "how it's calculated" (the Floor Area Analysis owns the breakdown); multi-lot/rezoning rows keep a compact calc toggle. Overbuilt flags inline via a `warn` slot.

**VERIFIED LIVE 2026-07-25** (Login + sandbox): Commercial rename in the layer tree, ★ best chips (BEST ASSEMBLAGE / BEST REZONE), the study-area rezone scenario row (`Rezone · study area → M1-4A/R8A`, SF 18,700→108,000, FAR 1.25→7.20) and its criteria calc all render. Two bugs found + fixed (commit cf6e5ef): amendmentYears printed as a raw float (it's YEARS-SINCE, converted to a calendar year now), and the bump line lost the current district for a lot inside a group (group branch passed null; now site-level district + site-level built FAR so it agrees with the row's grid).

1b-DONE (commit aaec66b): **ONE BOX** — a single lime box wraps scenarios + verdict + rezone fallback + the analysis dropdown (inner blocks de-bordered); the bar is renamed **"Site details"** and sits at the BOTTOM of the box.

**RESTRUCTURE COMPLETE (commit 9e83747, verified live 2026-07-25):** scenarios render as LEVELS with headings that only appear when the level has rows — "Site · as of right" → "Multi-lot" (portfolio/assemblage) → "Rezoning" (QRS + study area, adjacent since both are site-level) → "Rezoning + multi-lot". The per-row `divider` flag is gone. Helper `groupHead(label, first)` in PropertyCard.tsx. Full suite green (611); two stale tests refreshed (populate 72h→30d window; dev-site lens vs the investment-sale partition from 3881db7).

**PENDING Opportunity work (older list, item 1 now DONE):**
1. ~~RESTRUCTURE into levels~~ — DONE. (a) SITE AS-IS scenarios (the per-use rows) first, (b) rename "Site & Position · Zoning · Floor Area" → "**Site details**" as a dropdown of that stuff, placed with the site group, (c) a DIVIDER, then (d) the bigger plays: multi-lot (Portfolio, Assemblage), rezoning (QRS, study-area — `lib/studyArea.ts` exists), and rezoning+multi-lot combos.
2. **Simplify the multi-lot/rezoning CALC** (Login: "assemblage calculation is overly long — just drop down and show how the lots summate, compact, in place with the design"). The heavy `AssemblageBlock`/`PortfolioBlock` (groups.tsx) need replacing with a COMPACT per-lot summation table. BLOCKER: `AssemblageMember` (lib/assemblages.ts:58) is only `{bbl,address,owner}` — the per-lot `lotSf/existingSf/own` exist on the internal `Cand` (lines 344-360) but are dropped. Add those 3 fields to `AssemblageMember` + pass them through, then render a tight "lot A + lot B = combined" table.
2b. **DONE for assemblage (commit 79378e0):** the multi-lot calc is now a compact `lotSummation` table — each lot on its own row with `lot SF · FAR · % built`, footing to a Combined row. Exposed `lotSf/existingSf/remaining` on `AssemblageMember` (lib/assemblages.ts). **STILL TODO — portfolio:** `PortfolioMember` (lib/portfolios.ts:36) is also just `{bbl,address,owner}`; add the 3 per-lot fields (per-member `lotSf = num(LotArea)`, `existingSf = basis.existingSf`, `remaining = max(basis.permittedResSf,permittedOtherSf) − existingSf`, pushed at line ~292), then reuse `lotSummation` — BUT generalize it: portfolio totals use `remainingResidentialSf/remainingOtherSf` (NOT assemblage's `combinedRemainingResSf/OtherSf`), so pass a normalized `{members,label,parcels,lotSf,existingSf,combinedRemaining}` shape and a `label` for the header (portfolio = 1 owner).

2c. **DONE (commits through 4f43b0f):** study-area rezone is now its OWN scenario row (`buildoutRow` id "study-rezone", "Rezone · study area → <target district>") — SF extrapolated to the target FAR (`rezoneToFar × lot`), transition grid, and a calc dropdown = the criteria (**Transit** dist · **Zoning** = higher density/use allowed nearby, flags "(ULURP pending)" · **Activity** = active market for higher density/use) footing to the zoning-bump line (current district·FAR → target district·FAR, like QRS). Helper `rezoneStudyBody` (PropertyCard.tsx); prose note is now fallback-only. **ULURP is ALREADY live-checked** — `lib/rezoning.ts` `fetchAmendments` pulls DCP `nyzma` zoning-map-amendments (adopted + CERTIFIED in-ULURP) per territory; `pendingRezone` = certified-in-ULURP covers the block. So no ULURP source to build — it's live.

2c-orig. **STUDY-AREA REZONE = SPECULATIVE, must be JUSTIFIED (Login 2026-07-24).** QRS rezoning is an EXACT statutory calc (keep as-is). Study-area rezoning is a proposed upzone that needs justification — the calc dropdown should show THREE PILLARS then the conclusion: (1) **Transit** (proximity, e.g. "2-min walk to Court Sq-23 St"), (2) **Density** (a neighbor in the study area already got the higher zoning, e.g. "surrounding blocks rezoned to M1-4A/R8A, 7.2 FAR, LIC Neighborhood Plan 2025"), (3) **Activity** (nearby development momentum, e.g. "3 residential projects underway"). Then: "→ these justify a rezone to [target district] which allows [target FAR]." The prose already exists (the "REZONE BASIS: 8-BLOCK STUDY AREA — SPECULATIVE" paragraph from lib/studyArea.ts) — restructure it into the 3 labeled pillars + the target conclusion. `lib/studyArea.ts`/`lib/rezoning.ts` hold the signals.

2d. **GAP — rezone + multi-lot combined (port-qrs, asm-qrs rows):** currently their calc body shows ONLY the `lotSummation` (how the lots sum), NOT the QRS criteria justification. A combined row should show BOTH: the per-lot summation AND the combined-site rezone criteria checklist + bottom-line (district · FAR standard → FAR qrs). Compose the two in the body.
2e. **GAP — COMMERCIAL lane:** commercial USE scenarios render as normal buildoutRows (e.g. "Commercial"/"Manufacturing"), but (a) QRS/City-of-Yes is a RESIDENTIAL uplift, so a commercial rezone must go the STUDY-AREA path (2c), and (b) Login wants best-commercial highlighted per level (best-path, item 3). Also the Comparables "Commercial" lease lane is separately SHELVED ([[project_bkrea_commercial_comps]]).

2f. **TERMINOLOGY — rename "Other" → "Commercial" (Login 2026-07-24):** the non-residential lane is called "Other" in code/UI (`topOther`, the "Rezoning · Other" bucket, best-`other`). Login: if we're using residential/commercial terminology, this lane should read "**Commercial**." Rename user-facing labels (keep the internal key if risky). CAVEAT to confirm: "Other" currently spans commercial + community facility + manufacturing/industrial — so "Commercial" is cleaner but slightly broad; Login seems fine treating non-residential = commercial for the res-vs-commercial framing (incl. best-path item 3 "best commercial per level").

2f-DONE (commit ce1dda6): **"Other" → "Commercial" rename shipped** — layer toggle tree (Underbuilt + Rezoning), Opportunities filter headers, card group category tags (groups.tsx), and the site verdict sentence (opportunity.ts). All bucket KEYS keep the historical `other-` spelling, so stored classifications / saved filters / paint layers are untouched.

3-DONE (commit afa1a40): **BEST-PATH chips shipped** — `buildoutRow` takes `best?: string`, rendering a "★ best …" lime chip by the title. Site use rows → "best residential" / "best commercial" (from existing topRes/topOther); portfolio → "best portfolio"; assemblage → "best assemblage"; rezone rows compare QRS remaining vs study-area remaining (`bestRezone`) → "★ best rezone" on the winner. POSSIBLE POLISH: the study-area row doesn't set `lead` (accent color) even when it wins the chip.

3-orig. **BEST-PATH highlighting (Login idea):** mark the BEST option per level × use — best residential and best commercial at site / portfolio / assemblage / rezone level (rezone only if the site has it). Some highlighted chip/indicator ("★ best residential"). The code already computes `topRes`/`topOther` at site level (the old `lead`) — extend per group. Subtle, don't clutter.

**PARKED — AIR RIGHTS (Login 2026-07-24):** the buildable analysis does NOT yet account for air rights / TDR (transferable development rights), which can change the real remaining envelope. Login: "we still have to find a way to determine air rights… may be best to plug into PropertyScout and harvest from there." NOT NOW — "for now we aren't auditing." Revisit after the card designs + commercial comps. See [[project_bkrea_commercial_comps]].

**Sequencing (Login 2026-07-24):** finish the CARD DESIGNS, then do COMMERCIAL COMPARABLES ([[project_bkrea_commercial_comps]] — un-shelve). No auditing/air-rights until then. See [[project_bkrea_territory_intel]].
