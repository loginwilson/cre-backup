---
name: project-decoder-function-model
description: SETTLED 2026-08-16 — eleven noun functions closed by the account test; lexicon.canon() is the ONE normalizer; data tables = three parts (grammar/pipeline/checks); vocabulary is a measured ledger with mandatory denominators
metadata: 
  node_type: memory
  type: project
  originSessionId: 7c5a3ccb-a88e-40cd-a587-cc575cf7a400
  modified: 2026-08-16T20:03:39.808Z
---

THE ELEVEN FUNCTIONS (closed 2026-08-16, all nouns): IDENTITY · TITLE · OCCUPANCY ·
ENCUMBRANCE · ENVELOPE · ENTITLEMENT · PERMIT · ASBUILT · CAPITAL · VALUE · COST.
Closed by the ACCOUNT TEST: a function is real iff it maintains a state no other
function's table can store (map / ledgers / registers / conserved account / record /
series). `unresolved` is a STATE not a bucket — rising unresolved = a 12th trying to
be born. CONTEXT is STRUCK (derived context = inference over lineage; exogenous facts
belong to subject_type). Liens are CAPITAL not ENCUMBRANCE (claim on value ≠ limit on
use). A private height-cap covenant is ENCUMBRANCE (posts nothing to the capacity
account). POA = TITLE · signals · subject=entity (1.07M docs). Taxes: assessment→VALUE,
bill→CAPITAL, 421-a/J-51 abatement→CAPITAL with expiry terms.

`lexicon.canon(label)` in decoder/lexicon.py is THE one normalizer (register/legacy →
canonical, CONTEXT→None); detector keys are the lowercase of canonical labels. Never
build a local COLLAPSE map. Six detectors proven/weak; ENTITLEMENT·PERMIT·ASBUILT·
VALUE·COST have NO detector and NO ACRIS type — always `unread`, never 0%.

Data-tables doc (artifact dff… no — claude.ai/code/artifact/0fdbb50a-77d5-4c0a-8f39-58911b3e9f67,
generator scratchpad/gen_vocab.py) restructured to three parts: A event grammar
(subject/function/mode/quantity/term), B pipeline (document→claim→event→account→
inference; claims re-verify against the page, inferences recompute over lineage, NEVER
one table), C checks (vocabulary ledger · expected-vs-observed · completeness).
Vocabulary is the instrument layer for EVERY axis: each entry = patterns + status
(proven/weak/proposed/unread) + coverage WITH denominator + measured_against. OCCUPANCY
detector built by measurement (occupy_build.py): 95% recall / 9% leak on 151 docs, 5
eras; keep-rule must allow per-type mastery (terminate=100% TERL); `the premises`
rejected (55% leases but 100% DEVR / 38% MTGE). 6 of 8 residual misses = SUBL used for
MORTGAGE subordination — the register is wrong about SUBL. Mode has ZERO measured
vocabulary (inherited from source) — the emptiest, most load-bearing gap.

**Why:** functions route documents into stable tables; a wrong function set forces
exception columns forever, and an unmeasured reader is a blind spot wearing a checkmark.

**How to apply:** any new source/doc type: expected function is a PRIOR never a filter;
run ALL detectors; score present/absent/unread per function; new vocabulary only via a
measured bench (recall + per-type leak vs control types). Related: [[project_acris_decoder]],
[[feedback_confidence_backcheck]], [[feedback_bkrea_scale_failure]].
